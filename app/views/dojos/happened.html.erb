<table class="table table-striped">
  <caption>
    <h2>Dojos que já aconteceram!</h2>
  </caption>

  <thead>
    <tr>
      <th>Local</th>
      <th>Cidade</th>
      <th>Dia</th>
      <th>Limite de Pessoas</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @dojos.each do |dojo| %>
      <tr>
        <td><%= dojo.local.name %></td>
        <td><%= dojo.local.city %></td>
        <td><%= dojo.day %></td>
        <td><%= dojo.limit_people %></td>
        <td><%= link_to 'Detalhes', dojo_path(dojo.id)  %></td>
      </tr>
    <% end %>
  </tbody>
</table>
